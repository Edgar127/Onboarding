{
  "name": "deckgl-example-pure-js-mapbox",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "start-local": "vite --config ../vite.config.local.mjs"
  },
  "dependencies": {
    "@deck.gl/aggregation-layers": "file://root/deck.gl/modules/aggregation-layers/",
    "@deck.gl/core": "^8.8.0",
    "@deck.gl/layers": "^8.8.0",
    "@deck.gl/mapbox": "^8.8.0",
    "mapbox-gl": "^1.13.0"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
